{
  "name": "october",
  "description": "...",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "8.10"
  },
  "dependencies": {},
  "devDependencies": {},
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "( cd packages/app && yarn test ) && ( cd packages/blobstore-aws-dynamodb && yarn test )"
  }
}
